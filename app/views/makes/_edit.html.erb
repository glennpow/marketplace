<%= f.text_field :name, :required => true %>
  
<% if make.try(:image?) %>
  <%= image_tag make.image.url %>
<% end %>
 
<%= f.file_field :image %>

<%= enumeration_select(f, :production_status) %>

<%= f.text_area :description, :required => true, :preview => true, :hint => link_to_textile_hint %>

<% if make.valid_feature_types? %>
  <%= render_heading(tp(:feature, :scope => [ :marketplace ])) %>

  <%= feature_select(f, :features, :label => { :name => tp(:feature, :scope => [ :marketplace ]) }) %>
<% end %>