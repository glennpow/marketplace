<%= render :partial => 'organizations/show', :locals => { :organization => vendor.organization } %>

<%= render_heading(tp(:product, :scope => [ :marketplace ]), :actions => link_to(t(:view_all), vendor_prices_path(vendor))) %>

<%= render_field(vendor.prices(:limit => 10).map { |price| link_to_resource(price) }) %>
