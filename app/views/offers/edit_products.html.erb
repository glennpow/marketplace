<% title tp(:offer, :scope => [ :marketplace ]) %>

<%= render_heading(t(:products_for_object, :scope => [ :marketplace, :products ], :count => @indexer.collection.total_entries, :object => @offer.name)) %>

<%= render_indexer(@indexer) %>
